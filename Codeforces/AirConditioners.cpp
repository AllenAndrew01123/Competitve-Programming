#include<bits/stdc++.h>
using namespace std;
#define mod 1000000007
typedef long long ll;
void solve()
{
    ll n,k;
    ll a[k+1];
    ll temp[k+1];
    for(int i=1;i<=k;i++)
        cin>>a[i];
    for(int i=1;i<=k;i++)
        cin>>temp[i];
}
int main()
{
    cin.tie(0)->sync_with_stdio(0);
    int t;cin>>t;
    while(t--)
    {
        solve();
    }
}